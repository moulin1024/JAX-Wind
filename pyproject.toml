[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jaxwind"
version = "0.0.0"
description = "Functional JAX large-eddy simulation solver"
requires-python = ">=3.10"
dependencies = [
    "jax>=0.4.30",
    "scipy>=1.10",
    "tomli>=1.1.0; python_version < '3.11'",
]

[project.scripts]
jaxwind = "jaxwind.cli.main:main"

[project.optional-dependencies]
dev = ["pytest>=7", "ruff>=0.6"]
tools = ["h5py>=3.10"]
# The finite-volume solver's GPU pressure solve. The package lives in the
# external/jax-amg submodule; install it with 'pip install -e external/jax-amg'
# for a checkout, and note that it needs AmgX and a CUDA-enabled jaxlib.
amg = ["jaxamg>=0.1.4"]

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["jaxwind.windfarm.reference*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
# Keep the default suite deliberately small and architectural. Detailed
# backend, scaling, visualization, and publication-reproduction tests
# remain available with: pytest -o 'python_files=test_*.py'
python_files = [
    "test_runtime_contract.py",
    "test_case_schema.py",
    "test_architecture.py",
    "test_poisson.py",
    "test_operators.py",
    "test_low_mach.py",
    "test_cryogenic.py",
    "test_moisture.py",
    "test_moisture_config.py",
    "test_water_spray_benchmark.py",
    "test_water_droplet.py",
    "test_water_parcels.py",
    "test_open_scalar_flux.py",
    "test_water_projection.py",
    "test_surface.py",
    "test_turbine.py",
    "test_mann_inflow.py",
    "test_synthetic_main.py",
    "test_hitsz.py",
]

[tool.ruff]
line-length = 88
extend-exclude = ["src/jaxwind/windfarm/reference/openfast"]
