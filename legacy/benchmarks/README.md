# Historical benchmarks

These directories preserve reference data, plots, and case implementations
from before JAX-Wind adopted a direct solver/case boundary. They are retained
for scientific comparison, not as supported execution entry points.

The active case is
[`benchmark/PressureDrivenLASD`](../../benchmark/PressureDrivenLASD/README.md).
