# OpenFAST reference source

`openfast/` is the upstream [OpenFAST](https://github.com/OpenFAST/openfast)
repository pinned as a Git submodule. JAX-Wind does not import, build, link, or
execute this source tree. It is present only as an implementation and input
format reference for the native models in `jaxwind.windfarm`.
