# Convective ABL warmup

Dry, shear-free convective ABL driven by a fixed positive surface potential-
temperature flux:

```bash
jaxwind runners/abl_warmup_convective/config.toml --dry-run
jaxwind runners/abl_warmup_convective/config.toml
```
