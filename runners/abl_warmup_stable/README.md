# Stable ABL warmup

GABLS1-style stable ABL with prescribed surface cooling and plane-mean
Businger–Dyer surface fluxes:

```bash
jaxwind runners/abl_warmup_stable/config.toml --dry-run
jaxwind runners/abl_warmup_stable/config.toml
```
